# This is a configuration definition file in folder "/Users/asriraman/Documents/LTLMoP-ALL/LTLMoP/src/examples/SimpleSpec/configs".
# Format details are described at the beginning of each section below.


======== General Config ========

Actuator_Proposition_Mapping: # Mapping between actuator propositions and actuator handler functions

Initial_Truths: # Initially true propositions

Main_Robot: # The name of the robot used for moving in this config
OptimusSecundus

Name: # Configuration name
RobotSim

Region_Tags: # Mapping from tag names to region groups, for quantification
{}

Sensor_Proposition_Mapping: # Mapping between sensor propositions and sensor handler functions
move = share.DummySensorHandler.buttonPress(button_name='move', init_value=False)


======== Robot1 Config ========

ActuatorHandler: # Actuator handler file in robots/Type folder
iRobotCreate.IRobotCreateActuatorHandler()

CalibrationMatrix: # 3x3 matrix for converting coordinates, stored as lab->map
array([[   1.,    0.,  161.],
       [   0.,    1.,  128.],
       [   0.,    0.,    1.]])

DriveHandler: # Input value for robot drive handler, refer to file inside the handlers/drive folder
share.Drive.DifferentialDriveHandler(d=0.6)

InitHandler: # Input value for robot init handler, refer to the init file inside the handlers/robots/Type folder
iRobotCreate.IRobotCreateInitHandler(listenIP="0.0.0.0",broadCastIP="10.0.0.128",createPort=8865,beaglePort=8866,artagPort=8844,sonarPort=8833,buffer=1024)

LocomotionCommandHandler: # Input value for robot locomotion command handler, refer to file inside the handlers/robots/Type folder
iRobotCreate.IRobotCreateLocomotionCommandHandler()

MotionControlHandler: # Input value for robot motion control handler, refer to file inside the handlers/motionControl folder
share.MotionControl.VectorControllerHandler()

PoseHandler: # Input value for robot pose handler, refer to file inside the handlers/pose folder
share.Pose.WebcamPoseHandler()

RobotName: # Robot Name
OptimusSecundus

SensorHandler: # Sensor handler file in robots/Type folder
iRobotCreate.IRobotCreateSensorHandler()

Type: # Robot type
iRobotCreate

